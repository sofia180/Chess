services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: tcg
    ports:
      - "5432:5432"
    volumes:
      - tcg_pg:/var/lib/postgresql/data

volumes:
  tcg_pg:

