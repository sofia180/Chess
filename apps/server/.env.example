PORT=4000
NODE_ENV=development

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tcg?schema=public"

JWT_SECRET="replace_me"
JWT_EXPIRES_IN="7d"

# Telegram bot token (needed to verify initData hash)
TELEGRAM_BOT_TOKEN="123456:ABCDEF_replace_me"

# Fee + referral (defaults in code if unset)
PLATFORM_FEE_BPS=500
REFERRAL_FEE_SHARE_BPS=1000

# Admin (simple MVP)
ADMIN_API_KEY="replace_me_admin_key"

# CORS
WEB_ORIGIN="http://localhost:3000"
